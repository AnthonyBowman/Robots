# copy this shell script to the home directory - will probably transfer to Desktop, so up one level to home
sudo apt install git
git config --global user.name "Anthony"
git config --global user.email "brian@sherr.co.uk"
git version
git clone https://github.com/AnthonyBowman/Robots.git